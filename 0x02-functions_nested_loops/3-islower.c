#include "main.h"

/** 
 * _islower - check ir a character is lowercase
 *
 * Return: return 1 if lowercase
 */

int _islower(int c)
{
	int i;

	if (i >= 97 && i <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
